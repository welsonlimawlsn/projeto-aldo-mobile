import 'package:flutter/material.dart';

class AgendaTelefonicaPage extends StatelessWidget {
  const AgendaTelefonicaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agenda telefônica'),
      ),
    );
  }
}
